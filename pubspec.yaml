name: firetools_workspace
description: Monorepo workspace for Firebase Remote Config code generation tools
version: 0.0.0
publish_to: none

environment:
  sdk: ^3.6.0

workspace:
  - packages/remote_config_core
  - packages/firefreeze

dev_dependencies:
  lints: ^6.0.0
  test: ^1.28.0
